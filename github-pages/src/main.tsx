import React from "react";
import ReactDOM from "react-dom/client";
import ChangeLifeOS from "../../app/ChangeLifeOS";
import "../../app/globals.css";

ReactDOM.createRoot(document.getElementById("root")!).render(
  <React.StrictMode>
    <ChangeLifeOS />
  </React.StrictMode>,
);
